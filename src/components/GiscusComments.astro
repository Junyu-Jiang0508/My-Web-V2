---
/**
 * Giscus 评论区组件
 *
 * 配置方式：编辑 src/config.ts 中的 giscus.repoId 和 giscus.categoryId
 * 获取方式：访问 https://giscus.app/zh-CN 完成配置后复制
 */

import { giscus } from '../config';

interface Props {
  title?: string;
}

const { title = '学术讨论' } = Astro.props;
const { repo, repoId, category, categoryId, mapping, theme, lang, inputPosition, loading } = giscus;
---

<div id="comments-section" class="comments-section">
  <h3 class="comments-title">{title}</h3>
  <div class="comments-container">
    {repoId && categoryId ? (
      <script
        src="https://giscus.app/client.js"
        data-repo={repo}
        data-repo-id={repoId}
        data-category={category}
        data-category-id={categoryId}
        data-mapping={mapping}
        data-strict="0"
        data-reactions-enabled="1"
        data-emit-metadata="0"
        data-input-position={inputPosition}
        data-theme={theme}
        data-lang={lang}
        data-loading={loading}
        crossorigin="anonymous"
        async
      />
    ) : (
      <p class="comments-placeholder">
        评论区待配置。请在 <code>src/config.ts</code> 中填入从{' '}
        <a href="https://giscus.app/zh-CN" target="_blank" rel="noopener noreferrer">giscus.app</a>{' '}
        获取的 <code>repoId</code> 和 <code>categoryId</code>。
      </p>
    )}
  </div>
</div>

<style>
  .comments-section {
    margin-top: 3rem;
    padding-top: 2rem;
    border-top: 1px solid #e2e8f0;
    max-width: 42rem;
    margin-left: auto;
    margin-right: auto;
  }

  .comments-title {
    font-size: 1.25rem;
    font-weight: 600;
    color: #1e293b;
    margin-bottom: 1.25rem;
  }

  .comments-container {
    min-height: 120px;
  }

  .comments-placeholder {
    font-size: 0.875rem;
    color: #64748b;
    padding: 1rem;
    background: #f8fafc;
    border-radius: 0.5rem;
    border: 1px dashed #cbd5e1;
  }

  .comments-placeholder code {
    font-size: 0.8125rem;
    background: #e2e8f0;
    padding: 0.125rem 0.375rem;
    border-radius: 0.25rem;
  }

  .comments-placeholder a {
    color: #3b82f6;
    text-decoration: none;
  }

  .comments-placeholder a:hover {
    text-decoration: underline;
  }
</style>
